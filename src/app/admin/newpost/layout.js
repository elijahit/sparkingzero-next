export const metadata = {
  title: "Sparking Zero Italia - Aggiunta Post",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
