export const metadata = {
  title: "Sparking Zero Italia - Approva Post",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
