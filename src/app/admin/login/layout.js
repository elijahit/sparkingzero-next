export const metadata = {
  title: "Sparking Zero Italia - Login Admin",
};

export default function RootLayout({ children }) {
  return (
    <html lang="it">
      <body>{children}</body>
    </html>
  );
}
